# ─── WeaveLink HTTP Transport ────────────────────────────────────────────────
# Port the HTTP server listens on (default: 3001)
WEAVE_PORT=3001

# Host binding.
#   127.0.0.1 → local-only (recommended for development)
#   0.0.0.0   → all interfaces (required inside Docker)
WEAVE_HOST=127.0.0.1

# API key for authenticating HTTP requests.
# REQUIRED when running in HTTP mode (auth is enabled by default).
# Generate a key with:
#   node -e "const c='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';const b=new Uint8Array(32);crypto.getRandomValues(b);console.log(Array.from(b,x=>c[x%62]).join(''))"
WEAVE_API_KEY=CHANGE_ME_BEFORE_SHIPPING

# ─── LLM Provider ─────────────────────────────────────────────────────────────
# Uncomment the provider you use. Never commit real keys.
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# ─── Storage Provider ─────────────────────────────────────────────────────────
# Provider type: sqlite | mongodb | postgres | mysql
WEAVE_PROVIDER=sqlite

# SQLite — path to the database file (used when WEAVE_PROVIDER=sqlite)
WEAVE_SQLITE_PATH=./weave.db

# MongoDB — connection URI (used when WEAVE_PROVIDER=mongodb)
# WEAVE_MONGODB_URI=mongodb://localhost:27017/openweave

# PostgreSQL (used when WEAVE_PROVIDER=postgres)
# WEAVE_POSTGRES_URI=postgresql://user:password@localhost:5432/openweave

# MySQL (used when WEAVE_PROVIDER=mysql)
# WEAVE_MYSQL_URI=mysql://user:password@localhost:3306/openweave

# ─── Session Persistence ──────────────────────────────────────────────────────
# Directory for file-based session storage (default: .weave-sessions)
# WEAVE_SESSIONS_DIR=.weave-sessions

# ─── Development ──────────────────────────────────────────────────────────────
NODE_ENV=development
